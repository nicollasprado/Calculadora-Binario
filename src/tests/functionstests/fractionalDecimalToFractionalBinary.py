def convertFractionalDecimalToFractionalBinary(fractionalDecimal: str, bitsQuantity: int):
    return
    # fractionalDecimalFloat = float(f"0.{fractionalDecimal}")
    # bits = ""
    # i = 0
    # while i < bitsQuantity:
    #     bits += "1"
    #     i += 1
        
    # sum = 0.0
    # nextSum = 0.0
    # baseExpoent = -1
    # while sum < fractionalDecimalFloat:
    #     baseNum = 2 ** baseExpoent
    #     if (sum + baseNum == fractionalDecimalFloat):
    #     nextSum += 2 ** baseExpoent
    #     baseExpoent -= 1

# WORKING